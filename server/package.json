{
  "name": "server",
  "scripts": {
    "start": "nest start",
    "start:dev": "nest start --watch",
    "start:debug": "nest start --debug --watch",
    "build": "nest build"
  },
  "private": true,
  "dependencies": {

  },
  "devDependencies": {
  }
}
